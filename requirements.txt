# Use CPU-only torch wheels. Without this, the default CUDA torch build (~2 GB)
# exceeds Streamlit Community Cloud's image limit and the deploy fails. The CPU
# index also serves Windows/macOS wheels, so this line is safe everywhere.
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Core app ---
streamlit>=1.36.0

# --- Config / secrets ---
python-dotenv>=1.0.0

# --- Orchestration ---
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0

# --- LLM (Azure OpenAI GPT-4.1) ---
openai>=1.40.0

# --- RAG: embeddings + vector store ---
sentence-transformers>=3.0.0
torch                       # CPU build via the --extra-index-url above
# transformers v5 eagerly imports a vision model (zoedepth) that requires
# torchvision; install it so the embedding model imports cleanly even though
# this is a text-only project.
torchvision
chromadb>=0.5.0
# Chroma needs sqlite3 >= 3.35; Streamlit Cloud's bundled sqlite is too old, so
# swap in this modern build (see src/__init__.py). Linux-only — not needed for
# local Windows/macOS dev.
pysqlite3-binary; platform_system == "Linux"

# --- Document parsing ---
pypdf>=4.2.0

# --- Calculator (safe evaluator) ---
sympy>=1.12

# --- Evaluation ---
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
